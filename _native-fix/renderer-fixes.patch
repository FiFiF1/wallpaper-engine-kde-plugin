diff --git a/src/Audio/AudioCapture.cpp b/src/Audio/AudioCapture.cpp
new file mode 100644
index 0000000..2de20fb
--- /dev/null
+++ b/src/Audio/AudioCapture.cpp
@@ -0,0 +1,367 @@
+#include "Audio/AudioCapture.hpp"
+#include "Utils/Logging.h"
+
+#include <algorithm>
+#include <chrono>
+#include <cmath>
+#include <cstring>
+#include <mutex>
+#include <vector>
+
+// Header-only include. The miniaudio implementation is compiled exactly once,
+// in SoundManager.cpp via miniaudio-wrapper.hpp; these MA_NO_* defines must stay
+// identical to that translation unit's or the ma_* struct layouts would differ.
+#define MA_NO_WASAPI
+#define MA_NO_DSOUND
+#define MA_NO_WINMM
+#define MA_NO_COREAUDIO
+#define MA_NO_ENCODING
+#include <miniaudio/miniaudio.h>
+
+namespace wallpaper
+{
+
+namespace
+{
+constexpr std::size_t FFT_SIZE  = 1024;             // ~21ms at 48kHz
+constexpr std::size_t FFT_BINS  = FFT_SIZE / 2;
+constexpr ma_uint32   RATE      = 48000;
+constexpr auto        MIN_PERIOD = std::chrono::milliseconds(8);
+
+// Band edges in Hz, 16 log-spaced bands over the musically useful range.
+constexpr float BAND_EDGES[AUDIO_BANDS + 1] = { 30,   60,   95,   140,  200,  280,
+                                                400,  550,  760,  1050, 1450, 2000,
+                                                2800, 4000, 6000, 9000, 16000 };
+
+// Same range, 64 log-spaced bands - for consumers needing finer resolution
+// than the scene shader uniforms (see AudioCapture.hpp).
+constexpr float BAND_EDGES_WIDE[AUDIO_BANDS_WIDE + 1] = {
+    30.0f,    33.1f,    36.5f,    40.3f,    44.4f,    49.0f,    54.0f,    59.6f,
+    65.8f,    72.5f,    80.0f,    88.3f,    97.4f,    107.4f,   118.5f,   130.7f,
+    144.2f,   159.0f,   175.4f,   193.5f,   213.5f,   235.5f,   259.7f,   286.5f,
+    316.0f,   348.6f,   384.6f,   424.2f,   467.9f,   516.2f,   569.4f,   628.1f,
+    692.8f,   764.2f,   843.0f,   929.9f,   1025.8f,  1131.5f,  1248.2f,  1376.8f,
+    1518.8f,  1675.3f,  1848.1f,  2038.6f,  2248.7f,  2480.5f,  2736.2f,  3018.3f,
+    3329.4f,  3672.7f,  4051.2f,  4468.9f,  4929.6f,  5437.7f,  5998.3f,  6616.6f,
+    7298.7f,  8051.1f,  8881.0f,  9796.6f,  10806.4f, 11920.4f, 13149.3f, 14504.8f,
+    16000.0f,
+};
+
+// In-place iterative radix-2 FFT. `re`/`im` are FFT_SIZE long.
+void fft(float* re, float* im) {
+    // bit-reversal permutation
+    for (std::size_t i = 1, j = 0; i < FFT_SIZE; i++) {
+        std::size_t bit = FFT_SIZE >> 1;
+        for (; j & bit; bit >>= 1) j ^= bit;
+        j ^= bit;
+        if (i < j) {
+            std::swap(re[i], re[j]);
+            std::swap(im[i], im[j]);
+        }
+    }
+    for (std::size_t len = 2; len <= FFT_SIZE; len <<= 1) {
+        const double ang  = -2.0 * M_PI / (double)len;
+        const float  wr_d = (float)std::cos(ang);
+        const float  wi_d = (float)std::sin(ang);
+        for (std::size_t i = 0; i < FFT_SIZE; i += len) {
+            float wr = 1.0f, wi = 0.0f;
+            for (std::size_t k = 0; k < len / 2; k++) {
+                const float ur = re[i + k], ui = im[i + k];
+                const float vr = re[i + k + len / 2] * wr - im[i + k + len / 2] * wi;
+                const float vi = re[i + k + len / 2] * wi + im[i + k + len / 2] * wr;
+                re[i + k]               = ur + vr;
+                im[i + k]               = ui + vi;
+                re[i + k + len / 2]     = ur - vr;
+                im[i + k + len / 2]     = ui - vi;
+                const float nwr         = wr * wr_d - wi * wi_d;
+                wi                      = wr * wi_d + wi * wr_d;
+                wr                      = nwr;
+            }
+        }
+    }
+}
+} // namespace
+
+struct AudioCapture::Impl {
+    // Separate locks by role. `life_mtx` guards refcount + device lifetime;
+    // `mtx` guards only the sample ring and is taken by the realtime audio
+    // callback, so it must never be held while starting/stopping the device.
+    std::mutex life_mtx;
+    std::mutex mtx;                       // guards the sample ring
+    ma_context ctx {};
+    ma_device  dev {};
+    bool       ctx_ok { false };
+    bool       dev_ok { false };
+    bool       running { false };
+    int        refs { 0 };
+    bool       tried { false };           // only attempt device setup once per start
+
+    // Ring of interleaved stereo samples, most recent FFT_SIZE frames.
+    std::array<float, FFT_SIZE> ring_l {};
+    std::array<float, FFT_SIZE> ring_r {};
+    std::size_t                 wpos { 0 };
+    bool                        got_audio { false };
+
+    // Cached analysis output.
+    std::mutex                            out_mtx;
+    AudioSpectrum                         out_l {};
+    AudioSpectrum                         out_r {};
+    std::chrono::steady_clock::time_point last_analysis {};
+    bool                                  have_analysis { false };
+
+    // Independent cache for the wide spectrum: separate smoothing state (the
+    // fast-attack/slow-release memory in analyse_channel_into is per band
+    // count) and a separate due-check, so polling one resolution doesn't
+    // perturb the other's timing.
+    std::mutex                            out_mtx_wide;
+    AudioSpectrumWide                     out_l_wide {};
+    AudioSpectrumWide                     out_r_wide {};
+    std::chrono::steady_clock::time_point last_analysis_wide {};
+    bool                                  have_analysis_wide { false };
+
+    // Windowing table (Hann), precomputed.
+    std::array<float, FFT_SIZE> window {};
+
+    Impl() {
+        for (std::size_t i = 0; i < FFT_SIZE; i++)
+            window[i] = 0.5f * (1.0f - std::cos(2.0f * (float)M_PI * (float)i / (FFT_SIZE - 1)));
+    }
+
+    static void data_cb(ma_device* dev, void*, const void* in, ma_uint32 frames) {
+        auto* self = static_cast<Impl*>(dev->pUserData);
+        if (self == nullptr || in == nullptr) return;
+        const float* f = static_cast<const float*>(in);
+        std::lock_guard<std::mutex> lk(self->mtx);
+        for (ma_uint32 i = 0; i < frames; i++) {
+            self->ring_l[self->wpos] = f[i * 2 + 0];
+            self->ring_r[self->wpos] = f[i * 2 + 1];
+            self->wpos               = (self->wpos + 1) % FFT_SIZE;
+        }
+        if (frames > 0) self->got_audio = true;
+    }
+
+    // Open the monitor source of the DEFAULT SINK. Picking merely the first
+    // ".monitor" device grabs whichever output happens to be enumerated first
+    // (often an idle HDMI port) and captures nothing but silence.
+    bool start() {
+        if (running) return true;
+        if (tried && ! dev_ok) return false;
+        tried = true;
+
+        if (! ctx_ok) {
+            ma_backend backends[] = { ma_backend_pulseaudio, ma_backend_alsa };
+            if (ma_context_init(backends, 2, nullptr, &ctx) != MA_SUCCESS) {
+                LOG_ERROR("audio: context init failed; audio-reactive scenes will see silence");
+                return false;
+            }
+            ctx_ok = true;
+        }
+
+        ma_device_info* play = nullptr;
+        ma_device_info* cap  = nullptr;
+        ma_uint32       nplay = 0, ncap = 0;
+        if (ma_context_get_devices(&ctx, &play, &nplay, &cap, &ncap) != MA_SUCCESS) {
+            LOG_ERROR("audio: device enumeration failed");
+            return false;
+        }
+
+        char want[300];
+        want[0] = '\0';
+        for (ma_uint32 i = 0; i < nplay; i++) {
+            if (play[i].isDefault) {
+                std::snprintf(want, sizeof want, "%s.monitor", play[i].id.pulse);
+                break;
+            }
+        }
+
+        int pick = -1, first_mon = -1;
+        for (ma_uint32 i = 0; i < ncap; i++) {
+            const bool is_mon = std::strstr(cap[i].id.pulse, ".monitor") != nullptr;
+            if (is_mon && first_mon < 0) first_mon = (int)i;
+            if (want[0] != '\0' && std::strcmp(cap[i].id.pulse, want) == 0) pick = (int)i;
+        }
+        if (pick < 0) pick = first_mon;
+        if (pick < 0) {
+            LOG_ERROR("audio: no monitor source found; audio-reactive scenes will see silence");
+            return false;
+        }
+
+        ma_device_config cfg  = ma_device_config_init(ma_device_type_capture);
+        cfg.capture.pDeviceID = &cap[pick].id;
+        cfg.capture.format    = ma_format_f32;
+        cfg.capture.channels  = 2;
+        cfg.sampleRate        = RATE;
+        cfg.dataCallback      = &Impl::data_cb;
+        cfg.pUserData         = this;
+
+        if (ma_device_init(&ctx, &cfg, &dev) != MA_SUCCESS) {
+            LOG_ERROR("audio: capture device init failed");
+            return false;
+        }
+        dev_ok = true;
+        if (ma_device_start(&dev) != MA_SUCCESS) {
+            LOG_ERROR("audio: capture device start failed");
+            ma_device_uninit(&dev);
+            dev_ok = false;
+            return false;
+        }
+        running = true;
+        LOG_INFO("audio: capturing system output from \"%s\"", cap[pick].name);
+        return true;
+    }
+
+    void stop() {
+        if (dev_ok) {
+            ma_device_uninit(&dev);
+            dev_ok = false;
+        }
+        running = false;
+        tried   = false;
+        {
+            std::lock_guard<std::mutex> lk(mtx);
+            got_audio = false;
+            ring_l.fill(0.0f);
+            ring_r.fill(0.0f);
+            wpos = 0;
+        }
+        {
+            std::lock_guard<std::mutex> lk(out_mtx);
+            out_l.fill(0.0f);
+            out_r.fill(0.0f);
+            have_analysis = false;
+        }
+        std::lock_guard<std::mutex> lk(out_mtx_wide);
+        out_l_wide.fill(0.0f);
+        out_r_wide.fill(0.0f);
+        have_analysis_wide = false;
+    }
+
+    // Shared by both band counts: FFT the ring buffer once, then reduce into
+    // whichever band-edge table/output array the caller asks for.
+    template<std::size_t N>
+    void analyse_channel_into(const std::array<float, FFT_SIZE>& src, std::size_t start,
+                              const float (&band_edges)[N + 1], std::array<float, N>& prev) {
+        static thread_local std::vector<float> re, im;
+        re.assign(FFT_SIZE, 0.0f);
+        im.assign(FFT_SIZE, 0.0f);
+        for (std::size_t i = 0; i < FFT_SIZE; i++)
+            re[i] = src[(start + i) % FFT_SIZE] * window[i];
+        fft(re.data(), im.data());
+
+        const float bin_hz = (float)RATE / (float)FFT_SIZE;
+        for (std::size_t b = 0; b < N; b++) {
+            const auto k0 = (std::size_t)std::max(1.0f, band_edges[b] / bin_hz);
+            auto       k1 = (std::size_t)(band_edges[b + 1] / bin_hz);
+            k1            = std::min(k1, FFT_BINS - 1);
+            float peak    = 0.0f;
+            for (std::size_t k = k0; k <= k1 && k < FFT_BINS; k++) {
+                const float mag = std::sqrt(re[k] * re[k] + im[k] * im[k]) * (2.0f / FFT_SIZE);
+                peak            = std::max(peak, mag);
+            }
+            // Magnitude -> dB -> 0..1, matching the rough feel of WE's meters.
+            const float db = 20.0f * std::log10(peak + 1e-7f);
+            float       v  = (db + 62.0f) / 62.0f;
+            v              = std::clamp(v, 0.0f, 1.0f);
+            // Fast attack, slower release, so bars pop on transients but decay
+            // smoothly instead of flickering.
+            prev[b] = v > prev[b] ? v : prev[b] + (v - prev[b]) * 0.28f;
+        }
+    }
+
+    bool snapshot_ring(std::array<float, FFT_SIZE>& l, std::array<float, FFT_SIZE>& r,
+                       std::size_t& start) {
+        std::lock_guard<std::mutex> lk(mtx);
+        if (! got_audio) return false;
+        l     = ring_l;
+        r     = ring_r;
+        start = wpos; // oldest sample
+        return true;
+    }
+
+    void analyse() {
+        std::array<float, FFT_SIZE> l, r;
+        std::size_t                 start;
+        if (! snapshot_ring(l, r, start)) return;
+        std::lock_guard<std::mutex> lk(out_mtx);
+        analyse_channel_into<AUDIO_BANDS>(l, start, BAND_EDGES, out_l);
+        analyse_channel_into<AUDIO_BANDS>(r, start, BAND_EDGES, out_r);
+        have_analysis = true;
+    }
+
+    void analyse_wide() {
+        std::array<float, FFT_SIZE> l, r;
+        std::size_t                 start;
+        if (! snapshot_ring(l, r, start)) return;
+        std::lock_guard<std::mutex> lk(out_mtx_wide);
+        analyse_channel_into<AUDIO_BANDS_WIDE>(l, start, BAND_EDGES_WIDE, out_l_wide);
+        analyse_channel_into<AUDIO_BANDS_WIDE>(r, start, BAND_EDGES_WIDE, out_r_wide);
+        have_analysis_wide = true;
+    }
+};
+
+AudioCapture::AudioCapture(): m_impl(std::make_unique<Impl>()) {}
+AudioCapture::~AudioCapture() {
+    if (m_impl) {
+        m_impl->stop();
+        if (m_impl->ctx_ok) ma_context_uninit(&m_impl->ctx);
+    }
+}
+
+AudioCapture& AudioCapture::Instance() {
+    static AudioCapture inst;
+    return inst;
+}
+
+void AudioCapture::Acquire() {
+    std::lock_guard<std::mutex> lk(m_impl->life_mtx);
+    if (m_impl->refs++ == 0) m_impl->start();
+}
+
+void AudioCapture::Release() {
+    std::lock_guard<std::mutex> lk(m_impl->life_mtx);
+    if (m_impl->refs > 0 && --m_impl->refs == 0) m_impl->stop();
+}
+
+bool AudioCapture::active() const { return m_impl->running && m_impl->got_audio; }
+
+void AudioCapture::GetSpectrum(AudioSpectrum& left, AudioSpectrum& right) {
+    if (! m_impl->running) {
+        left.fill(0.0f);
+        right.fill(0.0f);
+        return;
+    }
+    const auto now = std::chrono::steady_clock::now();
+    bool       due = false;
+    {
+        std::lock_guard<std::mutex> lk(m_impl->out_mtx);
+        due = ! m_impl->have_analysis || (now - m_impl->last_analysis) >= MIN_PERIOD;
+        if (due) m_impl->last_analysis = now;
+    }
+    if (due) m_impl->analyse();
+
+    std::lock_guard<std::mutex> lk(m_impl->out_mtx);
+    left  = m_impl->out_l;
+    right = m_impl->out_r;
+}
+
+void AudioCapture::GetSpectrumWide(AudioSpectrumWide& left, AudioSpectrumWide& right) {
+    if (! m_impl->running) {
+        left.fill(0.0f);
+        right.fill(0.0f);
+        return;
+    }
+    const auto now = std::chrono::steady_clock::now();
+    bool       due = false;
+    {
+        std::lock_guard<std::mutex> lk(m_impl->out_mtx_wide);
+        due = ! m_impl->have_analysis_wide || (now - m_impl->last_analysis_wide) >= MIN_PERIOD;
+        if (due) m_impl->last_analysis_wide = now;
+    }
+    if (due) m_impl->analyse_wide();
+
+    std::lock_guard<std::mutex> lk(m_impl->out_mtx_wide);
+    left  = m_impl->out_l_wide;
+    right = m_impl->out_r_wide;
+}
+
+} // namespace wallpaper
diff --git a/src/Audio/CMakeLists.txt b/src/Audio/CMakeLists.txt
index c36fd68..5ad2932 100644
--- a/src/Audio/CMakeLists.txt
+++ b/src/Audio/CMakeLists.txt
@@ -3,6 +3,7 @@ set(LIB_NAME wpAudio)
 add_library(${LIB_NAME}
     STATIC
     SoundManager.cpp
+    AudioCapture.cpp
 )
 
 target_link_libraries(${LIB_NAME}
diff --git a/src/Audio/include/Audio/AudioCapture.hpp b/src/Audio/include/Audio/AudioCapture.hpp
new file mode 100644
index 0000000..4a442ee
--- /dev/null
+++ b/src/Audio/include/Audio/AudioCapture.hpp
@@ -0,0 +1,74 @@
+#pragma once
+// System-audio capture + spectrum analysis for audio-reactive scenes.
+//
+// Wallpaper Engine scenes that declare the AUDIOPROCESSING combo sample two
+// uniforms, `g_AudioSpectrum16Left[16]` and `g_AudioSpectrum16Right[16]`.
+// This captures whatever is playing on the default output (the PulseAudio /
+// PipeWire ".monitor" source of the default sink) and reduces it to those two
+// 16-band spectra.
+//
+// One capture device is shared process-wide: plasmashell runs a separate scene
+// per monitor, and opening one input device per scene would show up as several
+// recording streams and waste CPU. Scenes Acquire()/Release() a reference and
+// the device is only open while at least one audio-reactive scene is live.
+#include <array>
+#include <cstdint>
+#include <memory>
+
+namespace wallpaper
+{
+
+inline constexpr std::size_t AUDIO_BANDS = 16;
+using AudioSpectrum                      = std::array<float, AUDIO_BANDS>;
+
+// Wider spectrum for consumers that are not the fixed-16-band scene shader
+// uniforms - currently just the web-wallpaper QML bridge (WebAudioSpectrum),
+// whose JS-side callback convention expects >=64 samples.
+inline constexpr std::size_t AUDIO_BANDS_WIDE = 64;
+using AudioSpectrumWide                       = std::array<float, AUDIO_BANDS_WIDE>;
+
+class AudioCapture {
+public:
+    // Process-wide instance. Never null; safe to use even if capture fails
+    // (it then reports silence forever).
+    static AudioCapture& Instance();
+
+    // Ref-counted device lifetime. Acquire() starts capture on the first
+    // reference; Release() stops it when the last one goes away.
+    void Acquire();
+    void Release();
+
+    // Latest 16-band spectra, each roughly 0..1. Cheap to call many times per
+    // frame: the analysis is computed at most once per ~8ms and cached, so
+    // every node in every scene can call it freely. Returns silence when
+    // capture is unavailable.
+    void GetSpectrum(AudioSpectrum& left, AudioSpectrum& right);
+
+    // Same analysis, 64 log-spaced bands instead of 16. Independent cache
+    // from GetSpectrum() (see .cpp) so a consumer of one resolution doesn't
+    // pay for or invalidate the other's cache.
+    void GetSpectrumWide(AudioSpectrumWide& left, AudioSpectrumWide& right);
+
+    // True once a capture device is actually open and delivering samples.
+    bool active() const;
+
+    AudioCapture(const AudioCapture&)            = delete;
+    AudioCapture& operator=(const AudioCapture&) = delete;
+
+private:
+    AudioCapture();
+    ~AudioCapture();
+    struct Impl;
+    std::unique_ptr<Impl> m_impl;
+};
+
+// RAII helper: hold one of these for as long as a scene needs audio.
+class AudioCaptureRef {
+public:
+    AudioCaptureRef() { AudioCapture::Instance().Acquire(); }
+    ~AudioCaptureRef() { AudioCapture::Instance().Release(); }
+    AudioCaptureRef(const AudioCaptureRef&)            = delete;
+    AudioCaptureRef& operator=(const AudioCaptureRef&) = delete;
+};
+
+} // namespace wallpaper
diff --git a/src/Audio/miniaudio-wrapper.hpp b/src/Audio/miniaudio-wrapper.hpp
index 69bca22..0e17594 100644
--- a/src/Audio/miniaudio-wrapper.hpp
+++ b/src/Audio/miniaudio-wrapper.hpp
@@ -140,8 +140,13 @@ public:
         if (IsInited()) {
             LOG_INFO("uninit sound device");
         }
-        UnmountAll();
+        // Stop and join the device thread BEFORE releasing the channels.
+        // ma_device_uninit() waits for any in-flight audio callback to finish;
+        // dropping the channels first frees the decoders (and the scene-owned
+        // storage they read from) while that thread may still be inside
+        // NextPcmData -- observed as a SIGBUS there when a scene is torn down.
         ma_device_uninit(&m_device); // always do it
+        UnmountAll();
     }
     // bool IsStarted() const { return ma_device_is_started(&m_device); }
     // bool IsStopped() const { return ma_device_get_state(&m_device) == MA_STATE_STOPPED; }
diff --git a/src/CMakeLists.txt b/src/CMakeLists.txt
index 8dd28c9..cf49dde 100644
--- a/src/CMakeLists.txt
+++ b/src/CMakeLists.txt
@@ -1,6 +1,7 @@
 find_package(OpenGL REQUIRED)
 find_package(Threads REQUIRED)
 pkg_check_modules(LZ4 REQUIRED liblz4)
+pkg_check_modules(FFMPEG REQUIRED libavcodec libavformat libavutil libswscale)
 
 option(ENABLE_RENDERDOC "Build with renderdoc api" OFF)
 
@@ -45,7 +46,9 @@ add_library(
   WPShaderParser.cpp
   WPSceneParser.cpp
   WPShaderValueUpdater.cpp
+  WPJsScriptEngine.cpp
   WPTexImageParser.cpp
+  VideoTex.cpp
   WPSoundParser.cpp
   WPMdlParser.cpp
   WPPuppet.cpp
@@ -65,8 +68,8 @@ set(InteralLib
 target_link_libraries(
   ${PROJECT_NAME}
   PUBLIC wpUtils
-  PRIVATE ${InteralLib} nlohmann_json ${LZ4_LIBRARIES}
+  PRIVATE ${InteralLib} nlohmann_json qjs ${LZ4_LIBRARIES} ${FFMPEG_LIBRARIES}
           ${CMAKE_THREAD_LIBS_INIT} ${CMAKE_DL_LIBS})
 
-target_include_directories(${PROJECT_NAME} PUBLIC . Swapchain)
+target_include_directories(${PROJECT_NAME} PUBLIC . Swapchain PRIVATE ${FFMPEG_INCLUDE_DIRS})
 set_property(TARGET ${PROJECT_NAME} PROPERTY POSITION_INDEPENDENT_CODE ON)
diff --git a/src/Image.hpp b/src/Image.hpp
index ccefef4..677d980 100644
--- a/src/Image.hpp
+++ b/src/Image.hpp
@@ -65,6 +65,9 @@ struct Image : NoCopy, NoMove {
     ImageHeader       header;
     std::vector<Slot> slots;
     std::string       key;
+    // If this .tex payload was actually an MP4/H.264 video, the raw stream is
+    // kept here (shared) so the texture cache can drive a VideoPlayer for it.
+    std::shared_ptr<std::vector<uint8_t>> videoData;
 };
 
 } // namespace wallpaper
diff --git a/src/Scene/include/Scene/SceneMaterial.h b/src/Scene/include/Scene/SceneMaterial.h
index 9663388..e3020bc 100644
--- a/src/Scene/include/Scene/SceneMaterial.h
+++ b/src/Scene/include/Scene/SceneMaterial.h
@@ -3,6 +3,7 @@
 #include <vector>
 #include <memory>
 #include <unordered_set>
+#include <unordered_map>
 
 #include "SceneShader.h"
 #include "Type.hpp"
@@ -13,6 +14,13 @@ namespace wallpaper
 struct SceneMaterialCustomShader {
     std::shared_ptr<SceneShader> shader;
     ShaderValues                 constValues;
+    // GLSL uniform name -> SceneScript source, for constValues entries whose
+    // WP-Engine-authored JSON carried a "script" alongside its static
+    // fallback "value" (see WPMaterial.cpp). Run every frame by
+    // WPShaderValueUpdater via WPJsScriptEngine; constValues holds each
+    // entry's last-known value in the meantime (the static fallback until
+    // the script first runs, then whatever it last returned).
+    std::unordered_map<std::string, std::string> constValueScripts;
 };
 
 struct SceneMaterial {
diff --git a/src/SpecTexs.hpp b/src/SpecTexs.hpp
index 3edbd82..212711c 100644
--- a/src/SpecTexs.hpp
+++ b/src/SpecTexs.hpp
@@ -26,6 +26,13 @@ constexpr std::string_view WE_HALF_COMPO_BUFFER_PREFIX { "_rt_HalfCompoBuffer" }
 constexpr std::string_view WE_QUARTER_COMPO_BUFFER_PREFIX { "_rt_QuarterCompoBuffer" };
 constexpr std::string_view WE_FULL_COMPO_BUFFER_PREFIX { "_rt_FullCompoBuffer" };
 constexpr std::string_view WE_MIP_MAPPED_FRAME_BUFFER { "_rt_MipMappedFrameBuffer" };
+// Shadow atlas: packed depth maps for shadow-casting lights (LIGHTS_SHADOW_MAPPING).
+constexpr std::string_view WE_SHADOW_ATLAS { "_rt_shadowAtlas" };
+// "_alias_" names are engine-provided textures, not files on disk. The only one
+// scenes reference is the light cookie (LIGHTS_COOKIE), which modulates a
+// light's shape; white means "no cookie", i.e. the light is unmodulated.
+constexpr std::string_view WE_ALIAS_PREFIX { "_alias_" };
+constexpr std::string_view WE_UTIL_WHITE { "util/white" };
 
 constexpr std::string_view WE_EFFECT_PPONG_PREFIX { "_rt_effect_pingpong_" };
 constexpr std::string_view WE_EFFECT_PPONG_PREFIX_A { "_rt_effect_pingpong_a_" };
@@ -61,6 +68,10 @@ constexpr std::string_view G_ETVP { "g_EffectTextureProjectionMatrix" };
 constexpr std::string_view G_ETVPI { "g_EffectTextureProjectionMatrixInverse" };
 constexpr std::string_view G_LP { "g_LightsPosition" };
 constexpr std::string_view G_LCP { "g_LightsColorPremultiplied" };
+constexpr std::string_view G_LCR { "g_LightsColorRadius" };
+constexpr std::string_view G_LSKY { "g_LightSkylightColor" };
+constexpr std::string_view G_AS16L { "g_AudioSpectrum16Left" };
+constexpr std::string_view G_AS16R { "g_AudioSpectrum16Right" };
 
 constexpr std::string_view G_TIME { "g_Time" };
 constexpr std::string_view G_DAYTIME { "g_DayTime" };
diff --git a/src/VideoTex.cpp b/src/VideoTex.cpp
new file mode 100644
index 0000000..97e752b
--- /dev/null
+++ b/src/VideoTex.cpp
@@ -0,0 +1,181 @@
+#include "VideoTex.hpp"
+#include "Utils/Logging.h"
+#include <cstring>
+#include <chrono>
+
+extern "C" {
+#include <libavformat/avformat.h>
+#include <libavcodec/avcodec.h>
+#include <libswscale/swscale.h>
+#include <libavutil/imgutils.h>
+}
+
+namespace wallpaper {
+
+bool LooksLikeVideo(const uint8_t* data, size_t size) {
+    if (size < 12) return false;
+    return data[4] == 'f' && data[5] == 't' && data[6] == 'y' && data[7] == 'p';
+}
+
+namespace
+{
+struct MemBuf { const uint8_t* data; size_t size; size_t pos; };
+
+int read_cb(void* opaque, uint8_t* buf, int bufsz) {
+    auto* m = static_cast<MemBuf*>(opaque);
+    size_t remain = m->size - m->pos;
+    if (remain == 0) return AVERROR_EOF;
+    size_t n = (static_cast<size_t>(bufsz) < remain) ? static_cast<size_t>(bufsz) : remain;
+    std::memcpy(buf, m->data + m->pos, n);
+    m->pos += n;
+    return static_cast<int>(n);
+}
+int64_t seek_cb(void* opaque, int64_t offset, int whence) {
+    auto* m = static_cast<MemBuf*>(opaque);
+    if (whence == AVSEEK_SIZE) return static_cast<int64_t>(m->size);
+    int64_t np;
+    if (whence == SEEK_SET) np = offset;
+    else if (whence == SEEK_CUR) np = static_cast<int64_t>(m->pos) + offset;
+    else if (whence == SEEK_END) np = static_cast<int64_t>(m->size) + offset;
+    else return -1;
+    if (np < 0 || static_cast<size_t>(np) > m->size) return -1;
+    m->pos = static_cast<size_t>(np);
+    return np;
+}
+
+// A minimal open/decoder bundle over an in-memory MP4.
+struct Decoder {
+    MemBuf           mb;
+    AVIOContext*     avio { nullptr };
+    AVFormatContext* fmt { nullptr };
+    AVCodecContext*  ctx { nullptr };
+    SwsContext*      sws { nullptr };
+    int              vstream { -1 };
+    int              w { 0 }, h { 0 };
+
+    bool open(const uint8_t* data, size_t size) {
+        mb = { data, size, 0 };
+        const int BSZ = 1 << 16;
+        uint8_t* avio_buf = static_cast<uint8_t*>(av_malloc(static_cast<size_t>(BSZ)));
+        avio = avio_alloc_context(avio_buf, BSZ, 0, &mb, read_cb, nullptr, seek_cb);
+        fmt = avformat_alloc_context();
+        fmt->pb = avio;
+        if (avformat_open_input(&fmt, nullptr, nullptr, nullptr) < 0) return false;
+        if (avformat_find_stream_info(fmt, nullptr) < 0) return false;
+        vstream = av_find_best_stream(fmt, AVMEDIA_TYPE_VIDEO, -1, -1, nullptr, 0);
+        if (vstream < 0) return false;
+        AVCodecParameters* par = fmt->streams[vstream]->codecpar;
+        const AVCodec* dec = avcodec_find_decoder(par->codec_id);
+        if (! dec) return false;
+        ctx = avcodec_alloc_context3(dec);
+        avcodec_parameters_to_context(ctx, par);
+        if (avcodec_open2(ctx, dec, nullptr) < 0) return false;
+        w = ctx->width; h = ctx->height;
+        return true;
+    }
+    // Decode the next frame into `out` (RGBA8). Returns 1 on frame, 0 on EOF, -1 error.
+    int next(std::vector<uint8_t>& out, AVPacket* pkt, AVFrame* frame) {
+        while (true) {
+            int r = av_read_frame(fmt, pkt);
+            if (r < 0) return 0; // EOF / error -> caller loops
+            if (pkt->stream_index != vstream) { av_packet_unref(pkt); continue; }
+            int sr = avcodec_send_packet(ctx, pkt);
+            av_packet_unref(pkt);
+            if (sr < 0) continue;
+            int rr = avcodec_receive_frame(ctx, frame);
+            if (rr == AVERROR(EAGAIN)) continue;
+            if (rr < 0) return -1;
+            int fw = frame->width, fh = frame->height;
+            if (! sws)
+                sws = sws_getContext(fw, fh, static_cast<AVPixelFormat>(frame->format),
+                                     fw, fh, AV_PIX_FMT_RGBA, SWS_BILINEAR, nullptr, nullptr, nullptr);
+            out.resize(static_cast<size_t>(fw) * static_cast<size_t>(fh) * 4u);
+            uint8_t* dst[4] = { out.data(), nullptr, nullptr, nullptr };
+            int      dstst[4] = { fw * 4, 0, 0, 0 };
+            sws_scale(sws, frame->data, frame->linesize, 0, fh, dst, dstst);
+            return 1;
+        }
+    }
+    void rewind_() {
+        av_seek_frame(fmt, vstream, 0, AVSEEK_FLAG_BACKWARD);
+        avcodec_flush_buffers(ctx);
+    }
+    void close() {
+        if (sws) sws_freeContext(sws);
+        if (ctx) avcodec_free_context(&ctx);
+        if (fmt) avformat_close_input(&fmt);
+        if (avio) { av_freep(&avio->buffer); avio_context_free(&avio); }
+    }
+};
+} // namespace
+
+bool DecodeVideoFirstFrame(const uint8_t* data, size_t size,
+                           std::vector<uint8_t>& out_rgba, int& out_w, int& out_h) {
+    Decoder d;
+    if (! d.open(data, size)) { d.close(); LOG_ERROR("video: open failed"); return false; }
+    AVPacket* pkt = av_packet_alloc();
+    AVFrame*  frame = av_frame_alloc();
+    int r = d.next(out_rgba, pkt, frame);
+    out_w = d.w; out_h = d.h;
+    av_frame_free(&frame); av_packet_free(&pkt);
+    d.close();
+    return r == 1;
+}
+
+VideoPlayer::VideoPlayer(std::shared_ptr<const std::vector<uint8_t>> data) : m_data(std::move(data)) {
+    if (! m_data || m_data->empty()) return;
+    // Probe once so width/height are known immediately (for the initial texture).
+    Decoder probe;
+    if (probe.open(m_data->data(), m_data->size())) {
+        m_w = probe.w; m_h = probe.h; m_ok = true;
+    }
+    probe.close();
+    if (m_ok) m_thread = std::thread([this] { run(); });
+}
+
+VideoPlayer::~VideoPlayer() {
+    m_stop = true;
+    if (m_thread.joinable()) m_thread.join();
+}
+
+bool VideoPlayer::TakeFrame(std::vector<uint8_t>& out) {
+    std::lock_guard<std::mutex> lk(m_mtx);
+    if (! m_has_new) return false;
+    out = m_latest;
+    m_has_new = false;
+    return true;
+}
+
+void VideoPlayer::run() {
+    Decoder d;
+    if (! d.open(m_data->data(), m_data->size())) { d.close(); return; }
+    // Frame duration from the stream's average frame rate (fallback 30fps).
+    AVRational fr = d.fmt->streams[d.vstream]->avg_frame_rate;
+    double fps = (fr.num > 0 && fr.den > 0) ? (double)fr.num / (double)fr.den : 30.0;
+    if (fps < 1.0 || fps > 240.0) fps = 30.0;
+    auto frame_dur = std::chrono::duration<double>(1.0 / fps);
+
+    AVPacket* pkt = av_packet_alloc();
+    AVFrame*  frame = av_frame_alloc();
+    std::vector<uint8_t> buf;
+    auto next_t = std::chrono::steady_clock::now();
+    while (! m_stop) {
+        int r = d.next(buf, pkt, frame);
+        if (r == 0) { d.rewind_(); continue; }        // loop
+        if (r < 0) break;
+        {
+            std::lock_guard<std::mutex> lk(m_mtx);
+            m_latest.swap(buf);
+            m_has_new = true;
+        }
+        // pace to real time
+        next_t += std::chrono::duration_cast<std::chrono::steady_clock::duration>(frame_dur);
+        auto now = std::chrono::steady_clock::now();
+        if (next_t > now) std::this_thread::sleep_for(next_t - now);
+        else next_t = now;                            // fell behind; don't spiral
+    }
+    av_frame_free(&frame); av_packet_free(&pkt);
+    d.close();
+}
+
+} // namespace wallpaper
diff --git a/src/VideoTex.hpp b/src/VideoTex.hpp
new file mode 100644
index 0000000..227712a
--- /dev/null
+++ b/src/VideoTex.hpp
@@ -0,0 +1,57 @@
+#pragma once
+// Video textures: some Wallpaper Engine scenes use a playing MP4/H.264 as a
+// layer texture (the .tex payload is an ISO-BMFF/MP4 stream). This provides a
+// still-frame decode (for the initial texture) and a threaded streaming player
+// that keeps producing frames so the layer can animate.
+#include <cstdint>
+#include <cstddef>
+#include <vector>
+#include <memory>
+#include <thread>
+#include <mutex>
+#include <atomic>
+
+namespace wallpaper {
+
+// Does this buffer start with an MP4/ISO-BMFF 'ftyp' box?
+bool LooksLikeVideo(const uint8_t* data, size_t size);
+
+// Decode the first frame of an in-memory video to tightly-packed RGBA8.
+bool DecodeVideoFirstFrame(const uint8_t* data, size_t size,
+                           std::vector<uint8_t>& out_rgba, int& out_w, int& out_h);
+
+// Streaming player: owns a copy of the video, decodes frames on a worker thread
+// paced to the video's frame rate, and loops. The render loop polls TakeFrame()
+// once per rendered frame and uploads whatever is newest (dropping frames it is
+// too slow to show). All heavy work is off the render thread.
+class VideoPlayer {
+public:
+    // Shares `data` (no copy of the potentially-large stream).
+    VideoPlayer(std::shared_ptr<const std::vector<uint8_t>> data);
+    ~VideoPlayer();
+    VideoPlayer(const VideoPlayer&)            = delete;
+    VideoPlayer& operator=(const VideoPlayer&) = delete;
+
+    bool ok() const { return m_ok; }
+    int  width() const { return m_w; }
+    int  height() const { return m_h; }
+
+    // If a newer decoded frame is ready, copy it into `out` and return true;
+    // otherwise return false (leave `out` untouched). Non-blocking.
+    bool TakeFrame(std::vector<uint8_t>& out);
+
+private:
+    void run();
+
+    std::shared_ptr<const std::vector<uint8_t>> m_data;
+    std::atomic<bool>    m_ok { false };
+    std::atomic<bool>    m_stop { false };
+    int                  m_w { 0 }, m_h { 0 };
+
+    std::mutex           m_mtx;
+    std::vector<uint8_t> m_latest;      // guarded by m_mtx
+    bool                 m_has_new { false };
+    std::thread          m_thread;
+};
+
+} // namespace wallpaper
diff --git a/src/Vulkan/TextureCache.cpp b/src/Vulkan/TextureCache.cpp
index 4f76e34..56bd6e1 100644
--- a/src/Vulkan/TextureCache.cpp
+++ b/src/Vulkan/TextureCache.cpp
@@ -6,6 +6,8 @@
 #include "Util.hpp"
 
 #include "Image.hpp"
+#include "VideoTex.hpp"
+#include <array>
 #include "Core/MapSet.hpp"
 #include "Core/ArrayHelper.hpp"
 #include "Utils/AutoDeletor.hpp"
@@ -498,9 +500,48 @@ ImageSlotsRef TextureCache::CreateTex(Image& image) {
         m_device.handle().WaitIdle();
     }
     m_tex_map[image.key] = std::move(img_slots);
+
+    // If this .tex was actually a video, spin up a decoder and remember the
+    // (cache-owned) image so UpdateVideos() can re-upload frames to it. The
+    // static first frame already uploaded above stays as a fallback.
+    if (image.videoData && ! m_tex_map[image.key].slots.empty()) {
+        auto player = std::make_unique<VideoPlayer>(image.videoData);
+        if (player->ok()) {
+            m_video_texs.push_back(VideoTexEntry {
+                std::move(player), ImageParameters(m_tex_map[image.key].slots.at(0)) });
+        }
+    }
     return m_tex_map[image.key];
 }
 
+void TextureCache::UpdateVideos() {
+    if (m_video_texs.empty()) return;
+    if (! m_tex_cmd) allocateCmd();
+
+    thread_local std::vector<uint8_t> frame;
+    for (auto& vt : m_video_texs) {
+        if (! vt.player->TakeFrame(frame)) continue; // no new frame yet -> keep last
+        const u32 w    = (u32)vt.player->width();
+        const u32 h    = (u32)vt.player->height();
+        const u32 size = w * h * 4u;
+        if (w == 0 || h == 0 || frame.size() < size) continue;
+
+        VmaBufferParameters buf;
+        (void)CreateStagingBuffer(m_device.vma_allocator(), size, buf);
+        {
+            void* v_data;
+            VVK_CHECK(buf.handle.MapMemory(&v_data));
+            memcpy(v_data, frame.data(), size);
+            buf.handle.UnMapMemory();
+        }
+        std::array<BufferParameters, 1> bufs { BufferParameters(buf) };
+        std::array<VkExtent3D, 1>       exts { VkExtent3D { w, h, 1 } };
+        CopyImageData(
+            bufs, exts, m_device.graphics_queue().handle, m_tex_cmd, vt.image);
+        m_device.handle().WaitIdle();
+    }
+}
+
 void TextureCache::allocateCmd() {
     const auto& pool = m_device.cmd_pool();
     VVK_CHECK(pool.Allocate(1, VK_COMMAND_BUFFER_LEVEL_PRIMARY, m_tex_cmds));
@@ -544,6 +585,7 @@ TextureCache::TextureCache(const Device& device): m_device(device) {}
 TextureCache::~TextureCache() {};
 
 void TextureCache::Clear() {
+    m_video_texs.clear();
     m_tex_map.clear();
     m_query_texs.clear();
     m_query_map.clear();
diff --git a/src/Vulkan/include/Vulkan/TextureCache.hpp b/src/Vulkan/include/Vulkan/TextureCache.hpp
index a4814a2..8e30a39 100644
--- a/src/Vulkan/include/Vulkan/TextureCache.hpp
+++ b/src/Vulkan/include/Vulkan/TextureCache.hpp
@@ -9,6 +9,7 @@ namespace wallpaper
 {
 
 class Image;
+class VideoPlayer;
 
 namespace vulkan
 {
@@ -47,6 +48,10 @@ public:
                                                  VkImageTiling);
     ImageSlotsRef                    CreateTex(Image&);
 
+    // Advance any video-backed textures by one frame (called once per rendered
+    // frame). Cheap when there are none.
+    void                             UpdateVideos();
+
     std::optional<ImageParameters> Query(std::string_view key, TextureKey content_hash,
                                          bool persist = false);
 
@@ -63,6 +68,14 @@ private:
     const Device&                m_device;
     Map<std::string, ImageSlots> m_tex_map;
 
+    // Textures whose source .tex was actually a video: a player produces frames
+    // off-thread and we re-upload to the (owned-by-cache) image each frame.
+    struct VideoTexEntry {
+        std::unique_ptr<VideoPlayer> player;
+        ImageParameters              image;
+    };
+    std::vector<VideoTexEntry> m_video_texs;
+
     struct QueryTex {
         idx                index { 0 };
         bool               share_ready { false };
diff --git a/src/VulkanRender/SceneToRenderGraph.cpp b/src/VulkanRender/SceneToRenderGraph.cpp
index 3f1f36e..b9ffa97 100644
--- a/src/VulkanRender/SceneToRenderGraph.cpp
+++ b/src/VulkanRender/SceneToRenderGraph.cpp
@@ -100,9 +100,64 @@ static void ToGraphPass(SceneNode* node, std::string_view output, i32 imgId, Ext
     auto& rgraph = *extra.rgraph;
     auto& scene  = *extra.scene;
 
-    auto loadEffect = [node, &rgraph, &scene, &extra](SceneImageEffectLayer* effs) {
+    // 'output' gets redirected to the effect layer's first ping-pong buffer
+    // below (when the node's camera HasImgEffect()) so the base material
+    // renders into the effect chain instead of straight to the screen.
+    // Saved before that happens so loadEffect can still find the real
+    // destination if every effect on the layer fails to compile.
+    std::string_view final_output = output;
+
+    auto loadEffect = [node, &rgraph, &scene, &extra, final_output](SceneImageEffectLayer* effs) {
         effs->ResolveEffect(scene.default_effect_mesh, "effect");
 
+        if (effs->EffectCount() == 0) {
+            // Every effect declared on this layer failed to compile (see
+            // WPSceneParser.cpp's "effect '...' failed to load" - a hard
+            // GLSL error, e.g. a wallpaper author's shader doing integer
+            // modulo on a float, which Wallpaper Engine's own compiler
+            // tolerates but glslang correctly rejects). The base layer's
+            // own material already rendered correctly into the first
+            // ping-pong buffer (see 'output = imgeff->FirstTarget()'
+            // below) - normally the last successful effect relays that
+            // into the real destination; with none, that relay never
+            // happens and the layer's content is silently dropped,
+            // leaving whatever was behind it - nothing, for a full-screen
+            // effect layer, i.e. a solid black rect.
+            //
+            // Relay it through with a copy pass IF AND ONLY IF the two
+            // buffers are the same size. CopyPass (see CopyPass.cpp) always
+            // issues a vkCmdCopyImage sized to the SOURCE's full extent,
+            // with no scaling - valid only when src and dst match exactly.
+            // A ping-pong buffer is sized to the effect layer's own
+            // declared canvas (WPSceneParser.cpp's
+            // scene.renderTargets[effect_ppong_a] = {wpimgobj.size...}),
+            // which very often does NOT match the actual output resolution
+            // (confirmed live: 2560x1440 source into a 1280x720
+            // destination) - vkCmdCopyImage has no scaling capability, so
+            // that mismatch is an out-of-bounds copy region
+            // (VUID-vkCmdCopyImage-dstOffset-00150), which is exactly what
+            // caused this to reproducibly take the whole GPU context down
+            // with VK_ERROR_DEVICE_LOST instead of just failing cleanly.
+            // A real fix for the mismatched case needs a scaling blit
+            // (sample-based shader pass), which does not exist in this
+            // renderer yet - out of scope here. Falling back to the
+            // original silently-dropped behaviour for that case is
+            // strictly safer than a copy that can corrupt/hang the GPU.
+            auto sameSize = [&scene](std::string_view a, std::string_view b) {
+                auto ia = scene.renderTargets.find(std::string(a));
+                auto ib = scene.renderTargets.find(std::string(b));
+                if (ia == scene.renderTargets.end() || ib == scene.renderTargets.end())
+                    return false;
+                return ia->second.width == ib->second.width
+                    && ia->second.height == ib->second.height;
+            };
+            if (sameSize(effs->FirstTarget(), final_output)) {
+                rg::addCopyPass(rgraph, rg::createTexDesc(std::string(effs->FirstTarget())),
+                                rg::createTexDesc(std::string(final_output)));
+            }
+            return;
+        }
+
         for (usize i = 0; i < effs->EffectCount(); i++) {
             auto& eff     = effs->GetEffect(i);
             auto  cmdItor = eff->commands.begin();
diff --git a/src/VulkanRender/VulkanRender.cpp b/src/VulkanRender/VulkanRender.cpp
index 9fb7776..8ccfcdc 100644
--- a/src/VulkanRender/VulkanRender.cpp
+++ b/src/VulkanRender/VulkanRender.cpp
@@ -282,6 +282,10 @@ void VulkanRender::Impl::DestroyRenderingResource(RenderingResources& rr) {}
 void VulkanRender::Impl::drawFrame(Scene& scene) {
     if (! (m_inited && m_pass_loaded)) return;
 
+    // Advance video-backed textures (in-scene MP4 layers) one frame before we
+    // render. No-op when the scene has none.
+    m_device->tex_cache().UpdateVideos();
+
         // LOG_INFO("used ram: %fm", (m_device->GetUsage()/1024.0f)/1024.0f);
 
 #if ENABLE_RENDERDOC_API
diff --git a/src/WPSceneParser.cpp b/src/WPSceneParser.cpp
index 487c9f9..a0f8a28 100644
--- a/src/WPSceneParser.cpp
+++ b/src/WPSceneParser.cpp
@@ -55,8 +55,33 @@ struct ParseContext {
     std::shared_ptr<SceneNode> effect_camera_node;
     std::shared_ptr<SceneNode> global_camera_node;
     std::shared_ptr<SceneNode> global_perspective_camera_node;
+
+    // Every SceneNode created while parsing "objects", keyed by its WP-Engine
+    // JSON "id" - populated as each image/particle/light/group node is
+    // created, so a LATER object's "parent" reference can be resolved
+    // regardless of array order (WP Engine scenes commonly declare a
+    // content object before the group node it's parented under would be
+    // reached by array position). See AppendToParentOrRoot.
+    wallpaper::Map<i32, std::shared_ptr<SceneNode>> id_node_map;
 };
 
+// Attaches `node` to the SceneNode registered under `parent_id` (composing
+// its transform with that ancestor via SceneNode's existing
+// AppendChild/UpdateTrans machinery - see SceneNode.cpp), or to the scene
+// root if there's no such id (parent_id < 0, meaning the JSON had no
+// "parent" key at all, or referenced an id this parser never created a
+// node for). Every wpscene::WP*Object's "origin" field is otherwise
+// (incorrectly) treated as an absolute world-space coordinate - see fix 11
+// in _native-fix/README.md.
+void AppendToParentOrRoot(ParseContext& context, i32 parent_id,
+                          const std::shared_ptr<SceneNode>& node) {
+    if (parent_id >= 0 && context.id_node_map.count(parent_id) != 0) {
+        context.id_node_map.at(parent_id)->AppendChild(node);
+    } else {
+        context.scene->sceneGraph->AppendChild(node);
+    }
+}
+
 using WPObjectVar = std::variant<wpscene::WPImageObject, wpscene::WPParticleObject,
                                  wpscene::WPSoundObject, wpscene::WPLightObject>;
 
@@ -217,6 +242,13 @@ BlendMode ParseBlendMode(std::string_view str) {
 
 void ParseSpecTexName(std::string& name, const wpscene::WPMaterial& wpmat,
                       const WPShaderInfo& sinfo) {
+    // Engine-provided aliases are not files. A light with no cookie assigned
+    // must sample as white so it is left unmodulated; resolving to the real
+    // util/white asset keeps it on the normal texture path.
+    if (sstart_with(name, WE_ALIAS_PREFIX)) {
+        name = std::string(WE_UTIL_WHITE);
+        return;
+    }
     if (IsSpecTex(name)) {
         if (name == "_rt_FullFrameBuffer") {
             name = SpecTex_Default;
@@ -240,8 +272,28 @@ void ParseSpecTexName(std::string& name, const wpscene::WPMaterial& wpmat,
         } else if (sstart_with(name, WE_HALF_COMPO_BUFFER_PREFIX)) {
         } else if (sstart_with(name, WE_QUARTER_COMPO_BUFFER_PREFIX)) {
         } else if (sstart_with(name, WE_FULL_COMPO_BUFFER_PREFIX)) {
+        } else if (name == WE_SHADOW_ATLAS) {
+            // Resolved from the scene's render targets, created on demand.
         } else {
-            LOG_ERROR("unknown tex \"%s\"", name.c_str());
+            // Not one of the fixed, hardcoded names above - but that does NOT
+            // mean it is invalid. Effects (Bloom, depth-of-field/"Bokeh blur",
+            // Cutout Vignette, and the "compose" image option) declare their
+            // own render targets with an AUTHOR-CHOSEN label plus a
+            // runtime-unique suffix - "_rt_<label>_<address>", e.g.
+            // "_rt_coc_140567771445120" or "_rt_buffer1_<addr>" (see
+            // getAddr()/rtname a few hundred lines up, and
+            // WPImageObject.cpp's "compose" option for "_rt_FullCompoBuffer1").
+            // There is no fixed vocabulary of labels to whitelist here - by
+            // the time this runs, that effect-fbo parsing has ALREADY
+            // registered the name into pScene->renderTargets with correct
+            // dimensions, and the caller (a few lines below, in LoadMaterial)
+            // already does its own accurate pScene->renderTargets.count(name)
+            // check and logs "not found in render targets" for anything
+            // genuinely missing. Logging an "unknown tex" error here for
+            // every one of these dynamically-named-but-perfectly-valid
+            // targets was pure false-positive noise on every load of a scene
+            // using such an effect - the name is left untouched and still
+            // resolves correctly just below.
         }
     }
 }
@@ -336,8 +388,18 @@ bool LoadMaterial(fs::VFS& vfs, const wpscene::WPMaterial& wpmat, Scene* pScene,
         if (IsSpecTex(name)) {
             if (IsSpecLinkTex(name)) {
                 svData.renderTargets.push_back({ i, name });
+            } else if (name == WE_SHADOW_ATLAS && pScene->renderTargets.count(name) == 0) {
+                // Created lazily: only scenes with LIGHTS_SHADOW_MAPPING sample
+                // it, and an atlas is not cheap. Nothing renders depth into it
+                // yet, so it reads as "unoccluded" and geometry stays fully lit
+                // rather than failing to resolve.
+                pScene->renderTargets[std::string(WE_SHADOW_ATLAS)] = {
+                    .width = 2048, .height = 2048, .withDepth = true
+                };
+                svData.renderTargets.push_back({ i, name });
+                resolution = { 2048, 2048, 2048, 2048 };
             } else if (pScene->renderTargets.count(name) == 0) {
-                LOG_ERROR("%s not found in render targes", name.c_str());
+                LOG_ERROR("%s not found in render targets", name.c_str());
             } else {
                 svData.renderTargets.push_back({ i, name });
                 const auto& rt = pScene->renderTargets.at(name);
@@ -457,6 +519,10 @@ void LoadConstvalue(SceneMaterial& material, const wpscene::WPMaterial& wpmat,
             LOG_ERROR("ShaderValue: %s not found in glsl", name.c_str());
         } else {
             material.customShader.constValues[glname] = value;
+            auto scriptIt = wpmat.constantshadervalueScripts.find(name);
+            if (scriptIt != wpmat.constantshadervalueScripts.end()) {
+                material.customShader.constValueScripts[glname] = scriptIt->second;
+            }
         }
     }
 }
@@ -522,7 +588,8 @@ void InitContext(ParseContext& context, fs::VFS& vfs, wpscene::WPScene& sc) {
         gb["g_TexelSize"]     = std::array { 1.0f / 1920.0f, 1.0f / 1080.0f };
         gb["g_TexelSizeHalf"] = std::array { 1.0f / 1920.0f / 2.0f, 1.0f / 1080.0f / 2.0f };
 
-        gb["g_LightAmbientColor"] = sc.general.ambientcolor;
+        gb["g_LightAmbientColor"]  = sc.general.ambientcolor;
+        gb["g_LightSkylightColor"] = sc.general.skylightcolor;
         gb["g_NormalModelMatrix"] = ShaderValue::fromMatrix(Matrix4f::Identity());
     }
 
@@ -593,6 +660,7 @@ void ParseImageObj(ParseContext& context, wpscene::WPImageObject& img_obj) {
                                                  Vector3f(wpimgobj.angles.data()));
     LoadAlignment(*spImgNode, wpimgobj.alignment, { wpimgobj.size[0], wpimgobj.size[1] });
     spImgNode->ID() = wpimgobj.id;
+    context.id_node_map[wpimgobj.id] = spImgNode;
 
     SceneMaterial     material;
     WPShaderValueData svData;
@@ -886,7 +954,7 @@ void ParseImageObj(ParseContext& context, wpscene::WPImageObject& img_obj) {
             }
         }
     }
-    context.scene->sceneGraph->AppendChild(spImgNode);
+    AppendToParentOrRoot(context, wpimgobj.parent, spImgNode);
 }
 
 struct ParticleChildPtr {
@@ -1079,6 +1147,54 @@ void ParseLightObj(ParseContext& context, wpscene::WPLightObject& light_obj) {
     context.scene->sceneGraph->AppendChild(node);
 }
 
+// WP Engine scenes can declare purely-structural "group" objects - no
+// image/particle/sound/light key, existing only to give a set of children a
+// shared origin/rotation/scale other objects then parent under (see fix 11
+// in _native-fix/README.md - e.g. an entrance-animation clip + its voice
+// lines, grouped so they can all be positioned/triggered together). This
+// parser used to skip such objects entirely (the object-type dispatch below
+// only creates a WPObjectVar for the four content keys), so every one of
+// their children's "origin" was later read as an absolute world coordinate
+// instead of being composed with this shared one - the child's own JSON
+// origin is typically authored as an offset FROM the group, e.g. exactly
+// cancelling it out for a group meant to sit at world origin, which reads
+// as "meaningless, arbitrary-looking numbers" until you realize that.
+//
+// A group's own "visible" is intentionally not checked here (unlike
+// content objects) - it exists purely to anchor a coordinate frame, not to
+// render or play anything itself, so whether IT is "visible" doesn't
+// change whether creating it is correct; only its children's own
+// visibility matters, and that's already handled where each of those gets
+// parsed.
+void ParseGroupObjs(ParseContext& context, const nlohmann::json& objects) {
+    for (const auto& obj : objects) {
+        if (obj.contains("image") && ! obj.at("image").is_null()) continue;
+        if (obj.contains("particle") && ! obj.at("particle").is_null()) continue;
+        if (obj.contains("sound") && ! obj.at("sound").is_null()) continue;
+        if (obj.contains("light") && ! obj.at("light").is_null()) continue;
+        if (! obj.contains("id")) continue; // unreferenceable, nothing to gain by keeping it
+
+        i32 id = -1;
+        GET_JSON_NAME_VALUE_NOWARN(obj, "id", id);
+
+        std::array<float, 3> origin { 0.0f, 0.0f, 0.0f };
+        std::array<float, 3> angles { 0.0f, 0.0f, 0.0f };
+        std::array<float, 3> scale { 1.0f, 1.0f, 1.0f };
+        GET_JSON_NAME_VALUE_NOWARN(obj, "origin", origin);
+        GET_JSON_NAME_VALUE_NOWARN(obj, "angles", angles);
+        GET_JSON_NAME_VALUE_NOWARN(obj, "scale", scale);
+
+        auto node = std::make_shared<SceneNode>(Vector3f(origin.data()), Vector3f(scale.data()),
+                                                Vector3f(angles.data()));
+        node->ID() = id;
+        context.id_node_map[id] = node;
+
+        i32 parent_id = -1;
+        GET_JSON_NAME_VALUE_NOWARN(obj, "parent", parent_id);
+        AppendToParentOrRoot(context, parent_id, node);
+    }
+}
+
 template<typename T>
 void AddWPObject(std::vector<WPObjectVar>& objs, const nlohmann::json& json_obj, fs::VFS& vfs) {
     T wpobj;
@@ -1149,6 +1265,12 @@ std::shared_ptr<Scene> WPSceneParser::Parse(std::string_view scene_id, const std
 
     context.scene->scene_id = scene_id;
 
+    // Must run before the content-object loop below so a content object's
+    // "parent" (resolved via AppendToParentOrRoot inside ParseImageObj
+    // etc.) can already find its group node regardless of which one
+    // appears first in the JSON array.
+    ParseGroupObjs(context, json.at("objects"));
+
     WPShaderParser::InitGlslang();
 
     for (WPObjectVar& obj : wp_objs) {
diff --git a/src/WPShaderParser.cpp b/src/WPShaderParser.cpp
index 7c3eaec..ba4a4f4 100644
--- a/src/WPShaderParser.cpp
+++ b/src/WPShaderParser.cpp
@@ -305,7 +305,12 @@ inline std::string Preprocessor(const std::string& in_src, ShaderType type, cons
                       &res,
                       includer);
 
-    std::regex re_io(R"(.+\s(in|out)\s[\s\w]+\s(\w+)\s*;)", std::regex::ECMAScript);
+    // NOTE: the char class is [ \t\w] (space/tab/word), NOT [\s\w]. \s also
+    // matches newlines, which let this greedy quantifier span the whole
+    // preprocessed shader and recurse until libstdc++'s std::regex overflowed
+    // the stack (a hard crash on some scenes). GLSL interface declarations are
+    // single-line, so excluding the newline is identical for real shaders.
+    std::regex re_io(R"(.+\s(in|out)\s[ \t\w]+\s(\w+)\s*;)", std::regex::ECMAScript);
     for (auto it = std::sregex_iterator(res.begin(), res.end(), re_io);
          it != std::sregex_iterator();
          it++) {
diff --git a/src/WPShaderValueUpdater.cpp b/src/WPShaderValueUpdater.cpp
index 4a1025e..e5231f4 100644
--- a/src/WPShaderValueUpdater.cpp
+++ b/src/WPShaderValueUpdater.cpp
@@ -6,6 +6,7 @@
 #include "SpecTexs.hpp"
 #include "Core/ArrayHelper.hpp"
 #include "Utils/Algorism.h"
+#include "Audio/AudioCapture.hpp"
 
 #include <Eigen/Dense>
 #include <Eigen/Geometry>
@@ -17,6 +18,12 @@
 using namespace wallpaper;
 using namespace Eigen;
 
+// Defined here (not inline) because m_audio_ref holds a forward-declared type,
+// whose deleter must see the complete type.
+WPShaderValueUpdater::WPShaderValueUpdater(Scene* scene)
+    : m_scene(scene), m_jsEngine(std::make_unique<WPJsScriptEngine>()) {}
+WPShaderValueUpdater::~WPShaderValueUpdater() = default;
+
 void WPShaderValueUpdater::FrameBegin() {
     /*
         using namespace std::chrono;
@@ -26,6 +33,8 @@ void WPShaderValueUpdater::FrameBegin() {
             (((cTime->tm_hour * 60) + cTime->tm_min) * 60 + cTime->tm_sec) / (24.0f * 60.0f
        * 60.0f);
     */
+    m_jsEngine->BeginFrame((float)m_scene->frameTime, (float)m_scene->elapsingTime);
+
     double new_time    = m_mouseDelayedTime + m_scene->frameTime;
     new_time           = new_time > m_parallax.delay ? m_parallax.delay : new_time;
     m_mouseDelayedTime = new_time;
@@ -72,6 +81,13 @@ void WPShaderValueUpdater::InitUniforms(SceneNode* pNode, const ExistsUniformOp&
     info.has_TEXELSIZEHALF    = existsOp(G_TEXELSIZEHALF);
     info.has_SCREEN           = existsOp(G_SCREEN);
     info.has_LP               = existsOp(G_LP);
+    info.has_LCR              = existsOp(G_LCR);
+    info.has_AS16L            = existsOp(G_AS16L);
+    info.has_AS16R            = existsOp(G_AS16R);
+
+    // A scene only captures system audio if some shader actually samples the
+    // spectrum; the device stays closed for every other wallpaper.
+    if (info.has_AS16L || info.has_AS16R) RequestAudio();
 
     std::accumulate(begin(info.texs), end(info.texs), 0, [&existsOp](uint index, auto& value) {
         value.has_resolution = existsOp(WE_GLTEX_RESOLUTION_NAMES[index]);
@@ -235,6 +251,48 @@ void WPShaderValueUpdater::UpdateUniforms(SceneNode* pNode, sprite_map_t& sprite
         updateOp(G_LP, lights);
         updateOp(G_LCP, lights_color);
     }
+
+    if (info.has_LCR) {
+        // vec4 per light: rgb = light colour, w = radius (4 light slots).
+        std::array<float, 16> lights_color_radius { 0 };
+        uint                  i = 0;
+        for (auto& l : m_scene->lights) {
+            if (i == 4) break;
+            const auto& color            = l->color();
+            lights_color_radius[i * 4]     = color[0];
+            lights_color_radius[i * 4 + 1] = color[1];
+            lights_color_radius[i * 4 + 2] = color[2];
+            lights_color_radius[i * 4 + 3] = l->radius();
+            i++;
+        }
+        updateOp(G_LCR, lights_color_radius);
+    }
+
+    if (info.has_AS16L || info.has_AS16R) {
+        AudioSpectrum left {}, right {};
+        AudioCapture::Instance().GetSpectrum(left, right);
+        if (info.has_AS16L) updateOp(G_AS16L, std::span<const float> { left.data(), left.size() });
+        if (info.has_AS16R) updateOp(G_AS16R, std::span<const float> { right.data(), right.size() });
+    }
+
+    // SceneScript-driven constant shader values (see SceneMaterial.h,
+    // WPMaterial.cpp). Keyed per (node, glsl uniform name) rather than just
+    // the uniform name, so two layers that happen to use the identical
+    // script text each still get their own independent module-scope state
+    // (e.g. two separately-timed fade-ins must not share one elapsedTime).
+    for (auto& [glname, script] : material->customShader.constValueScripts) {
+        auto& value = material->customShader.constValues[glname];
+        std::vector<float> v(value.data(), value.data() + value.size());
+        std::string key = std::to_string(reinterpret_cast<uintptr_t>(pNode)) + ":" + glname;
+        if (m_jsEngine->RunUpdate(key, script, v)) {
+            value = ShaderValue(v);
+        }
+        updateOp(glname, value);
+    }
+}
+
+void WPShaderValueUpdater::RequestAudio() {
+    if (! m_audio_ref) m_audio_ref = std::make_unique<AudioCaptureRef>();
 }
 
 void WPShaderValueUpdater::SetNodeData(void* nodeAddr, const WPShaderValueData& data) {
diff --git a/src/WPShaderValueUpdater.hpp b/src/WPShaderValueUpdater.hpp
index 3d5729c..621d785 100644
--- a/src/WPShaderValueUpdater.hpp
+++ b/src/WPShaderValueUpdater.hpp
@@ -13,11 +13,13 @@
 #include "Core/MapSet.hpp"
 #include "SpriteAnimation.hpp"
 #include "WPPuppet.hpp"
+#include "WPJsScriptEngine.hpp"
 
 namespace wallpaper
 {
 
 class Scene;
+class AudioCaptureRef;
 
 struct WPUniformInfo {
     bool has_MI { false };
@@ -38,6 +40,9 @@ struct WPUniformInfo {
     bool has_TEXELSIZEHALF { false };
     bool has_SCREEN { false };
     bool has_LP { false };
+    bool has_LCR { false };
+    bool has_AS16L { false };
+    bool has_AS16R { false };
 
     struct Tex {
         bool has_resolution { false };
@@ -63,8 +68,8 @@ struct WPCameraParallax {
 
 class WPShaderValueUpdater : public IShaderValueUpdater {
 public:
-    WPShaderValueUpdater(Scene* scene): m_scene(scene) {}
-    virtual ~WPShaderValueUpdater() {}
+    WPShaderValueUpdater(Scene* scene);
+    virtual ~WPShaderValueUpdater();
 
     void FrameBegin() override;
 
@@ -80,7 +85,20 @@ public:
     void SetScreenSize(i32 w, i32 h) override { m_screen_size = { (float)w, (float)h }; }
 
 private:
+    // Opens the shared system-audio capture the first time a shader in this
+    // scene is found to sample the spectrum; the reference keeps it open for
+    // this scene's lifetime.
+    void RequestAudio();
+
     Scene*               m_scene;
+    std::unique_ptr<AudioCaptureRef> m_audio_ref;
+    // Runs any constValueScripts entries (see SceneMaterial.h) once per
+    // frame from UpdateUniforms(); always constructed (not lazy like
+    // m_audio_ref) since even an unused QuickJS runtime is cheap and a
+    // per-material null check at every UpdateUniforms() call is not worth
+    // the ~1 avoided allocation for the (common) case of a scene with no
+    // scripted values at all.
+    std::unique_ptr<WPJsScriptEngine> m_jsEngine;
     WPCameraParallax     m_parallax;
     double               m_dayTime { 0.0f };
     std::array<float, 2> m_texelSize { 1.0f / 1920.0f, 1.0f / 1080.0f };
diff --git a/src/WPTexImageParser.cpp b/src/WPTexImageParser.cpp
index ec396e4..5088150 100644
--- a/src/WPTexImageParser.cpp
+++ b/src/WPTexImageParser.cpp
@@ -1,4 +1,6 @@
 #include "WPTexImageParser.hpp"
+#include "VideoTex.hpp"
+#include <vector>
 
 #include "Type.hpp"
 #include "WPCommon.hpp"
@@ -110,7 +112,16 @@ void LoadHeader(fs::IBinaryStream& file, ImageHeader& header) {
 
     header.count = file.ReadInt32();
 
-    if (header.extraHeader["texb"].val == 3) header.type = static_cast<ImageType>(file.ReadInt32());
+    // Image-container textures store their source format (a FreeImage FIF value,
+    // e.g. PNG=13) right after the image count; TEXB0004 added one further field
+    // after it. The original code only read the format, and only for TEXB0003, so
+    // every TEXB0004 texture misaligned - containers read garbage mipmap sizes and
+    // failed, raw ones read as empty. Read the format for TEXB0003+ and skip the
+    // extra field for TEXB0004+.
+    if (header.extraHeader["texb"].val >= 3) {
+        header.type = static_cast<ImageType>(file.ReadInt32());
+        if (header.extraHeader["texb"].val >= 4) file.ReadInt32();
+    }
 }
 
 void SetHeaderPow2(ImageHeader& header, i32 mip_0_w, i32 mip_0_h) {
@@ -184,8 +195,37 @@ std::shared_ptr<Image> WPTexImageParser::Parse(const std::string& name) {
                     return nullptr;
                 }
             }
+            // is video (MP4/H.264 packed into a scene texture) - some WE scenes
+            // use a playing video as a layer; decode a frame instead of treating
+            // the stream as raw pixels (which showed as noise).
+            if (LooksLikeVideo((const uint8_t*)result, (usize)src_size)) {
+                // Keep the raw stream so a VideoPlayer can animate the layer.
+                img.videoData = std::make_shared<std::vector<uint8_t>>(
+                    (const uint8_t*)result, (const uint8_t*)result + (usize)src_size);
+                std::vector<uint8_t> rgba;
+                int                  vw = 0, vh = 0;
+                if (DecodeVideoFirstFrame((const uint8_t*)result, (usize)src_size, rgba, vw, vh)) {
+                    mipmap.width  = vw;
+                    mipmap.height = vh;
+                    if (i_mipmap == 0) {
+                        img_slot.width  = vw;
+                        img_slot.height = vh;
+                        SetHeaderPow2(img.header, vw, vh);
+                    }
+                    img.header.format = TextureFormat::RGBA8;
+                    src_size          = vw * vh * 4;
+                    mipmap.data       = ImageDataPtr(new uint8_t[(usize)src_size],
+                                               [](uint8_t* d) { delete[] d; });
+                    std::copy(rgba.begin(), rgba.end(), mipmap.data.get());
+                } else {
+                    LOG_ERROR("video texture decode failed");
+                    mipmap.data = ImageDataPtr(new uint8_t[(usize)src_size],
+                                               [](uint8_t* d) { delete[] d; });
+                    std::copy(result, result + src_size, mipmap.data.get());
+                }
+            }
             // is image container
-            if (img.header.extraHeader["texb"].val == 3 && img.header.type != ImageType::UNKNOWN) {
+            else if (img.header.extraHeader["texb"].val >= 3 && img.header.type != ImageType::UNKNOWN) {
                 int32_t w, h, n;
                 auto*   data =
                     stbi_load_from_memory((const unsigned char*)result, src_size, &w, &h, &n, 4);
diff --git a/src/wpscene/WPImageObject.cpp b/src/wpscene/WPImageObject.cpp
index 75580f8..39d734b 100644
--- a/src/wpscene/WPImageObject.cpp
+++ b/src/wpscene/WPImageObject.cpp
@@ -146,6 +146,7 @@ bool WPImageObject::FromJson(const nlohmann::json& json, fs::VFS& vfs) {
     GET_JSON_NAME_VALUE_NOWARN(jImage, "fullscreen", fullscreen);
 	GET_JSON_NAME_VALUE_NOWARN(json, "name", name);
 	GET_JSON_NAME_VALUE_NOWARN(json, "id", id);
+	GET_JSON_NAME_VALUE_NOWARN(json, "parent", parent);
 	GET_JSON_NAME_VALUE_NOWARN(json, "colorBlendMode", colorBlendMode);
 	if(!fullscreen) {
 		GET_JSON_NAME_VALUE(json, "origin", origin);	
diff --git a/src/wpscene/WPImageObject.h b/src/wpscene/WPImageObject.h
index 3e40257..00cd43d 100644
--- a/src/wpscene/WPImageObject.h
+++ b/src/wpscene/WPImageObject.h
@@ -60,6 +60,9 @@ public:
     };
     bool                       FromJson(const nlohmann::json&, fs::VFS&);
     int32_t                    id { 0 };
+    // -1 means "no parent" (WP Engine JSON simply omits the key for a
+    // root-level object) - a real id would never be negative.
+    int32_t                    parent { -1 };
     std::string                name;
     std::array<float, 3>       origin { 0.0f, 0.0f, 0.0f };
     std::array<float, 3>       scale { 1.0f, 1.0f, 1.0f };
diff --git a/src/wpscene/WPMaterial.cpp b/src/wpscene/WPMaterial.cpp
index 5fd9367..5b9afa3 100644
--- a/src/wpscene/WPMaterial.cpp
+++ b/src/wpscene/WPMaterial.cpp
@@ -22,6 +22,9 @@ void WPMaterialPass::Update(const WPMaterialPass& p) {
     for(const auto& el:p.constantshadervalues) {
         constantshadervalues[el.first] = el.second;
     }
+    for(const auto& el:p.constantshadervalueScripts) {
+        constantshadervalueScripts[el.first] = el.second;
+    }
     for(const auto& el:p.combos) {
         combos[el.first] = el.second;
     }
@@ -40,6 +43,9 @@ void WPMaterial::MergePass(const WPMaterialPass& p) {
     for(const auto& el:p.constantshadervalues) {
         constantshadervalues[el.first] = el.second;
     }
+    for(const auto& el:p.constantshadervalueScripts) {
+        constantshadervalueScripts[el.first] = el.second;
+    }
     for(const auto& el:p.combos) {
         combos[el.first] = el.second;
     }
@@ -61,6 +67,17 @@ bool WPMaterialPass::FromJson(const nlohmann::json& json) {
             GET_JSON_VALUE(jC.key(), name);
             GET_JSON_VALUE(jC.value(), value);
             constantshadervalues[name] = value;
+            // A SceneScript-driven value looks like {"value": [...], "script":
+            // "..."} - the fallback above already reads "value"; separately
+            // capture "script" (previously silently dropped, so any wallpaper
+            // relying on it - an animated fade, a delayed reveal - just froze
+            // at the static value forever) so WPShaderValueUpdater can run it
+            // every frame instead.
+            if(jC.value().is_object() && jC.value().contains("script")) {
+                std::string script;
+                GET_JSON_VALUE(jC.value().at("script"), script);
+                constantshadervalueScripts[name] = script;
+            }
         }
     }
     if(json.contains("combos")) {
@@ -113,6 +130,11 @@ bool WPMaterial::FromJson(const nlohmann::json& json) {
             GET_JSON_VALUE(jC.key(), name);
             GET_JSON_VALUE(jC.value(), value);
             constantshadervalues[name] = value;
+            if(jC.value().is_object() && jC.value().contains("script")) {
+                std::string script;
+                GET_JSON_VALUE(jC.value().at("script"), script);
+                constantshadervalueScripts[name] = script;
+            }
         }
     }
     if(jContent.contains("combos")) {
diff --git a/src/wpscene/WPMaterial.h b/src/wpscene/WPMaterial.h
index de8354f..c078109 100644
--- a/src/wpscene/WPMaterial.h
+++ b/src/wpscene/WPMaterial.h
@@ -24,6 +24,12 @@ public:
     std::vector<std::string>                            textures;
     std::unordered_map<std::string, int32_t>            combos;
     std::unordered_map<std::string, std::vector<float>> constantshadervalues;
+    // A constantshadervalue entry can carry a SceneScript "script" alongside
+    // its static "value" fallback (e.g. {"value": [1], "script": "..."}) -
+    // WPShaderValueUpdater runs it every frame via WPJsScriptEngine instead
+    // of using the static value verbatim. Keyed the same as
+    // constantshadervalues; only present for entries that actually have one.
+    std::unordered_map<std::string, std::string>        constantshadervalueScripts;
     std::string                                         target;
     std::vector<WPMaterialPassBindItem>                 bind;
 };
@@ -40,6 +46,7 @@ public:
     std::vector<std::string>                            textures;
     std::unordered_map<std::string, int32_t>            combos;
     std::unordered_map<std::string, std::vector<float>> constantshadervalues;
+    std::unordered_map<std::string, std::string>        constantshadervalueScripts;
 
     bool use_puppet { false };
 };
diff --git a/third_party/CMakeLists.txt b/third_party/CMakeLists.txt
index 190e213..5fbb8ea 100644
--- a/third_party/CMakeLists.txt
+++ b/third_party/CMakeLists.txt
@@ -21,3 +21,10 @@ set(ENABLE_GLSLANG_JS OFF)
 set(ENABLE_HLSL OFF)
 set(ENABLE_OPT OFF)
 add_subdirectory(glslang EXCLUDE_FROM_ALL)
+
+# quickjs-ng (MIT) - JS engine for Wallpaper Engine's SceneScript
+# (constantshadervalue.script) feature. Static "qjs" lib target only;
+# EXCLUDE_FROM_ALL keeps its CLI/test executables out of the default build.
+set(QJS_ENABLE_INSTALL OFF)
+set(QJS_BUILD_EXAMPLES OFF)
+add_subdirectory(quickjs EXCLUDE_FROM_ALL)
